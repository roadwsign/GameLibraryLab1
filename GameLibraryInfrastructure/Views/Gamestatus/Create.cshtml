@model GameLibraryDomain.Model.Gamestatus

@{
    ViewData["Title"] = "Додати статусу";
}

<h1>Додати</h1>

<h4>Статус</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Statusname" class="control-label"></label>
                <input asp-for="Statusname" class="form-control" />
                <span asp-validation-for="Statusname" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Створити" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-secondary ms-2">Повернутися до списку</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
